# React Notes

## `import`ing Images as path

```js
import reactImg from "./assets/react-core-concepts.png";
```

When building project, explicitly tells the build tool that this js file references this image file.

Unlike hard-coding the path string to the image, this `import` prevents image file getting lost during build process.
